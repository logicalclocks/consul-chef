datacenter = "lc"
domain = "<%= node['consul']['domain'] %>"
data_dir = "<%= node['consul']['data_dir'] %>"
retry_join = <%= @masters %>
enable_local_script_checks = true
performance = {
    raft_multiplier = 1
}

ports = {
   http = <%= node['consul']['http_api_port'] %>
}